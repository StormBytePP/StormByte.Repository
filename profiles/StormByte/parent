# StormByte features (order matters: optimizations after base as it overrides CFLAGS)
StormByte:features/optimizations
StormByte:features/llvm
StormByte:features/security
StormByte:features/silent

# Bugfixes is added at the first selectable tier (glibc/musl) so it propagates to all descendants